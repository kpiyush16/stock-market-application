import { StockExchangeList } from './stock-exchange-list';

describe('StockExchangeList', () => {
  it('should create an instance', () => {
    expect(new StockExchangeList()).toBeTruthy();
  });
});
